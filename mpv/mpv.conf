# Keep the player open after the file ends (useful for the tiling script)
no-video
keep-open=no
save-position-on-quit
# Hide the window decorations (optional, looks better with tiling)
no-border

# Start with a specific volume
volume=80
